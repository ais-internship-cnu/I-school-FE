import { Rating, Button, Typography, Grid, TextField } from "@mui/material"
import 'styles/review-register.css'

const registerButton = () => {
    return(
        
        <Button variant='contained' fullWidth className='submit-button'>
        평가하기
        </Button>

    )
}

export default registerButton