export type BottomSheetCourse = {
    courseId: number;
    courseName: string;
    professor: string;
    major: string;
    grade: number;
    rating: number;
};

export type BottomSheetCourses = {
    bottomSheetCourseList: BottomSheetCourse[];
  }

